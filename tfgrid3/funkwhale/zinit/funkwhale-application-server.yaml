exec: "sh -c 'cd /srv/funkwhale/api && . /srv/funkwhale/config/.env && /srv/funkwhale/venv/bin/gunicorn config.asgi:application --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 127.0.0.1:5000'"
after:
    - funkwhale-celery-beat

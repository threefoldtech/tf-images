exec: |
  bash -c '
  ufw default deny incoming
  ufw default allow outgoing
  ufw allow ssh
  ufw allow 80
  ufw allow 8001
  ufw allow 8002
  ufw limit ssh
  ufw --force enable
  '
oneshot: true
after:
  - sshd